specVersion: "v1beta"
serviceId: "saadhana-rn"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "saadhana-rn-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
